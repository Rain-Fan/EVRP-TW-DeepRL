# Data

- `samples/`: tiny version-controlled instances used by tests and examples.
- `raw/`: downloaded benchmark files; ignored by Git.
- `generated/`: synthetic instances; ignored by Git.

Every external dataset needs a source URL, license or usage note, original
instance name, checksum, and preprocessing command.

